# react-dom--ssr-example

A reproduction showing how componentWillMount is invoked with ReactDOMServer APIs

## Getting starter

- Run `yarn`
- Run `yarn start`
  - Notice how `I was invoked` is logged to the console
